  <!-- PRESENTATION -->
  <%= render "section_presentation" %>

  <!-- SKILLS -->
  <%= render "skills" %>

  <!-- EXPERIENCES -->
  <%= render "experiences" %>

  <!-- ABOUT -->
  <%= render "about" %>

  <!-- PROJECTS -->
  <%= render "projects" %>

  <!-- TESTIMONIALS -->
  <%= render "testimonials" %>

  <!-- CONTACT -->
  <%= render "contact" %>
